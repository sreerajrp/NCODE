# NCODE
Ncode is a visual web builder that generates HTML and CSS from structured project data. Users create elements, define hierarchy, and apply styles in the interface, while the system dynamically regenerates the page code.
